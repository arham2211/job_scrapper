# Database Configuration (Local PostgreSQL)
# User created 'job-scrapper' DB, so adapting the provided example URL to use that name.
DATABASE_URL=postgresql+asyncpg://postgres:arham@localhost:5433/job-scraper
DIRECT_DATABASE_URL=postgresql+psycopg2://postgres:arham@localhost:5433/job-scraper
